name: dnsolve
description: Pure Dart library that provides an easy way to perform DNS lookups
version: 0.5.0
topics: [network, http, dns]

environment:
  sdk: ">=3.0.0 <4.0.0"

dev_dependencies:
  lint: ^2.1.2
  test: ^1.21.0

dependencies:
  http: ^1.1.0
